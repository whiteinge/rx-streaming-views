/**
Register a new "module" with the app

Allows a central place to declare things like routes, middleware, stores,
views, tasks, etc that can all be collected and managed centrally.
**/
export const REGISTER = 'register';
export const UNREGISTER = 'unregister';
